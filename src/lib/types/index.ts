/**
 * Domain types for LabelVerify
 * 
 * This module will contain TypeScript interfaces and types for:
 * - ImageItem (id, file, name, previewUrl, size)
 * - ApplicationGroup (id, name, images[])
 * - ApplicationValues (brand, classType, abvOrProof, netContents, producer, country)
 * - ExtractedValues (same shape + governmentWarning)
 * - FieldStatus enum: Pass/Fail/NeedsReview/Missing/NotProvided
 * - ApplicationResult (overall + field results + warning block)
 * - Report (id, createdAt, mode, apps[], summary counts)
 */

export {};
